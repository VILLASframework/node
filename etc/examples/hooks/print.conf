@include "../nodes/signal_generator.conf"

paths = (
	{
		in = "signal_node"
		out = "signal_node"

		hooks = (
			{
				type = "print",

				output = "print_output_file.log"
				format = "villas.human"
				prefix = "[file_node] "
			}
		)
	}
)

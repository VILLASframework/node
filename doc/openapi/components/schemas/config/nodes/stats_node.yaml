# yaml-language-server: $schema=http://json-schema.org/draft-07/schema
---

allOf:
- type: object
  properties:
    rate:
      type: number
      description: A rate in Hz at which the statistics are generated by this node.

    in:
      type: object
      required:
      - signals
      properties: 
        signals:
          type: array
          items:
            $ref: ./signals/stats_signal.yaml

- $ref: ../node.yaml
